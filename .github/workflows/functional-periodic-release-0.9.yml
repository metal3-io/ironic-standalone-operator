name: Periodic Functional Tests release-0.9

on:
  schedule:
  # Run every day at 03:20 UTC (it is recommended to avoid running at the start of the hour)
  - cron: '20 3 * * *'
  workflow_dispatch:

permissions: {}

jobs:
  functional-tests:
    if: github.repository == 'metal3-io/ironic-standalone-operator'
    uses: ./.github/workflows/functional.yml
    with:
      ref: release-0.9
      fail-fast: false
    permissions:
      checks: write
      contents: read
